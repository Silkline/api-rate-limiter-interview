# ⚠️ Sample solution — interviewer reference only

**Candidates: do not open this folder.** Reading the solution defeats the purpose of the exercise.

Reference Rust solution passing core, extra-credit, and hard-mode (concurrency) tests.

**To verify against the tests:** copy `lib.rs` over `../src/lib.rs`, then from `rust/`:

```bash
cargo test -- --test-threads=1 --include-ignored
```
