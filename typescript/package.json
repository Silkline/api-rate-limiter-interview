{
  "name": "api-rate-limiter-typescript",
  "version": "1.0.0",
  "description": "API rate limiter interview - TypeScript",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
