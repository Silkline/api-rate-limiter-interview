# ⚠️ Sample solution — interviewer reference only

**Candidates: do not open this folder.** Reading the solution defeats the purpose of the exercise.

Reference Python solution passing core, extra-credit, and hard-mode (concurrency) tests.

**To verify against the tests:** copy `rate_limiter.py` over `../rate_limiter.py`, then from `python/`:

```bash
HARD_MODE=1 pytest -v
```
